name: rust_lib_photonix_camera
description: DSLR-quality photos via on-device computational photography and AI.
version: 0.1.0+1
publish_to: none

environment:
    sdk: ">=3.10.0 <4.0.0"

dependencies:
    flutter:
        sdk: flutter

    flutter_rust_bridge: "2.11.1"

    camera: "^0.11.0"
    permission_handler: "^11.4.0"

    flutter_riverpod: "^2.6.1"
    riverpod_annotation: "^2.6.1"

    go_router: "^14.6.3"

    provider: "^6.1.2"
    cached_network_image: "^3.4.1"
    path_provider: "^2.1.5"
    path: "^1.9.1"

    # image_gallery_saver: "^2.0.3"
    gal: "^2.3.0"

    logger: "^2.4.0"

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: "^5.0.0"
    riverpod_generator: "^2.6.1"
    build_runner: "^2.4.15"
    integration_test:
        sdk: flutter
